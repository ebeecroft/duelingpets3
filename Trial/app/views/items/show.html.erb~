<% provide(:title, "Item: Attributes of the item for purchase!") %>
<% provide(:keywords, "item, attributes, purchase") %>
<% provide(:description, "Where users to get together to socialize, roleplay or even chat.") %>
<p id="notice"><%= notice %></p>
<div class="pagetext">
   <div class="inner">Name: <%= @item.name %></div>
   <div class="inner"><%= button_to 'Edit', edit_item_path(@item), method: :get %></div>
   <div class="inner"><%= button_to 'Destroy', @item, method: :delete, data: { confirm: 'Are you sure?' } %></div>
   <p><%= image_tag @item.ipicture_url(:thumb).to_s %></p>
   <p>Created on: <%= @item.created_on.strftime("%B-%d-%Y") %></p>
   <p>Description: <%= @item.description %></p>
   <br>
   <p><%= link_to 'Back', items_path %></p>
</div>
