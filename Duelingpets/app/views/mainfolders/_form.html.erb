<%= form_for([@user, @mainfolder]) do |f| %>
  <% if @mainfolder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mainfolder.errors.count, "error") %> prohibited this mainfolder from being saved:</h2>

      <ul>
      <% @mainfolder.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br>
  <div class="pagetext">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="pagetext">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="pagetext">
    <%= f.submit %>
  </div>
  <br>
<% end %>
